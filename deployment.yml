spec:
  template:
    spec:
      serviceAccountName: todo-sa
      containers:
        - name: web
          image: your-registry-or-local/todo:latest
          ports:
            - containerPort: 8000
